// Simular el proceso de carga y redirección automática
        window.addEventListener('load', function() {
            // Después de 3 segundos, redirigir a la siguiente página
            setTimeout(function() {
                // Aquí puedes cambiar la URL de destino
                window.location.href = 'reiniciar2.html';

            }, 7000);
        });